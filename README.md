# UAL-Operations-Parser
M365 UAL parser for BEC investigations. Decodes raw AuditData JSON, extracts subjects, paths, inbox rules, and message IDs per operation type, and exports a clean XLSX + HTML report.
